<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <LinearLayout
        android:id="@+id/linearLayout1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="22dp" >

        <TextView
            android:id="@+id/lblMeasurement"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/staticLabel_measure" />

        <TextView
            android:id="@+id/lblCurrentMeasurement"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="20dp"
            android:text="@string/voltages" />

    </LinearLayout>

    <Button
        android:id="@+id/cButton"
        style="@style/AppTheme"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="65dp"
        android:layout_toRightOf="@+id/vButton"
        android:text="@string/cButton"
        android:textSize="10sp"
        android:textStyle="italic" />

    <Button
        android:id="@+id/aButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/cButton"
        android:layout_alignBottom="@+id/cButton"
        android:layout_toRightOf="@+id/cButton"
        android:text="@string/aButton"
        android:textSize="10sp"
        android:textStyle="italic" />

    <Button
        android:id="@+id/vButton"
        style="@style/AppTheme"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/cButton"
        android:layout_alignBottom="@+id/cButton"
        android:layout_alignParentLeft="true"
        android:text="@string/vButton"
        android:textSize="10sp"
        android:textStyle="italic" />

    <Button
        android:id="@+id/pButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/vButton"
        android:layout_toRightOf="@+id/aButton"
        android:text="@string/pButton"
        android:textSize="10sp"
        android:textStyle="italic" />

</RelativeLayout>